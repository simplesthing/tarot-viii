import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import DealCard from './deal-card';
import { default as colors } from '../../theme/colors';

<Meta
    title="atoms/DealCard"
    component={DealCard}
    args={{
        cardIndex: 21,
        spreadIndex: 1,
        reversed: false,
        dealt: false,
        onPress: () => ({})
    }}
    argTypes={{
        onPress: { action: 'clicked', description: 'event handler for press' }
    }}
    decorators={[
        Story => (
            <View
                style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                    padding: 50
                }}>
                <Story />
            </View>
        )
    ]}
/>

export const Template = args => <DealCard {...args} />;

# ShuffleCard

Card that randomly shuffles its position while isShuffling is true

<Canvas>
    <Story
        name="DealCard"
        parameters={{
            backgrounds: {
                default: 'default',
                values: [{ name: 'default', value: colors.silver_sand.shadow }]
            }
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

## ArgsTable

<ArgsTable story="DealCard" />
