import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import Deck from './deck';
import { default as colors } from '../../theme/colors';
import { StyleSheet, View } from 'react-native';

<Meta
    title="atoms/Deck"
    component={Deck}
    args={{
        onPress: () => ({})
    }}
    argTypes={{
        onPress: { action: 'clicked', description: 'event handler for press' }
    }}
    decorators={[
        Story => (
            <View
                style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                    padding: 50
                }}>
                <Story />
            </View>
        )
    ]}
/>

export const Template = args => <Deck {...args} />;

# Deck

Large card representing the deck at the beginning of a reading

<Canvas>
    <Story
        name="Deck"
        parameters={{
            backgrounds: {
                default: 'default',
                values: [{ name: 'default', value: colors.silver_sand.shadow }]
            }
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

## ArgsTable

<ArgsTable story="Deck" />
