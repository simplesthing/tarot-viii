import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import ReadingCarousel from './reading-carousel';
import { default as colors } from '../../theme/colors';
import reading from '../../../assets/data/mockReading';

<Meta
    title="molecules/ReadingCarousel"
    component={ReadingCarousel}
    args={{
        navigationEvent: index => ({}),
        width: 720,
        height: 695,
        data: {
            title: 'Storybook reading',
            notes: 'Taking notes',
            creationTme: 'Fri Dec 17 2021 13:32:04 GMT-0800 (PST)',
            reading: reading
        }
    }}
    argTypes={{
        navigationEvent: { action: 'clicked', description: 'event handler for press' }
    }}
    decorators={[
        Story => (
            <View
                style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                    padding: 50
                }}>
                <Story />
            </View>
        )
    ]}
/>

export const Template = args => <ReadingCarousel {...args} />;

# ReadingCarousel

Carousel to display reading cards and text meanings

<Canvas>
    <Story
        name="ReadingCarousel"
        parameters={{
            backgrounds: {
                default: 'default',
                values: [{ name: 'default', value: colors.silver_sand.shadow }]
            }
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

## ArgsTable

<ArgsTable story="ReadingCarousel" />
