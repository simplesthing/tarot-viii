import { Canvas, Meta, Story } from '@storybook/addon-docs';
import Start from './start';

<Meta
    title="molecules/Start"
    component={Start}
    args={{ onStart: () => ({}) }}
    argTypes={{
        onStart: { action: 'clicked', description: 'event handler for press' }
    }}
    decorators={[
        Story => (
            <View
                style={{
                    position: 'relative'
                }}>
                <Story />
            </View>
        )
    ]}
/>

# Start

Start screen

<Canvas>
    <Story name="Start">
        <Start />
    </Story>
</Canvas>
