import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import ForgotPassword from './forgot-password';
import { View } from 'react-native';
import { default as colors } from '../../theme/colors';

<Meta
    title="molecules/ForgotPassword"
    component={ForgotPassword}
    args={{
        buttonPress: () => ({}),
        error: {}
    }}
    argTypes={{
        buttonPress: { action: 'send email and password' }
    }}
    decorators={[
        Story => (
            <View
                style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                    padding: 50,
                    backgroundColor: colors.silver_sand.light
                }}>
                <Story />
            </View>
        )
    ]}
/>

export const Template = args => <ForgotPassword {...args} />;

# ForgotPassword

Forgot password form

<Canvas>
    <Story
        name="ForgotPassword"
        parameters={{
            backgrounds: {
                default: 'default',
                values: [{ name: 'default', value: colors.silver_sand.shadow }]
            }
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

## ArgsTable

<ArgsTable story="ForgotPassword" />
