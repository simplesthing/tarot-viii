import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { View } from 'react-native';
import EmailPassword from './email-password';
import { default as colors } from '../../theme/colors';

<Meta
    title="molecules/EmailPassword"
    component={EmailPassword}
    args={{
        buttonLabel: 'Login',
        buttonPress: () => ({}),
        error: {}
    }}
    argTypes={{
        buttonPress: { action: 'send email and password' },
        buttonLabel: { string: 'Login' }
    }}
    decorators={[
        Story => (
            <View
                style={{
                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                    padding: 50,
                    backgroundColor: colors.silver_sand.light
                }}>
                <Story />
            </View>
        )
    ]}
/>

export const Template = args => <EmailPassword {...args} />;

# EmailPassword

Email and password form for logging in

<Canvas>
    <Story
        name="EmailPassword"
        parameters={{
            backgrounds: {
                default: 'default',
                values: [{ name: 'default', value: colors.silver_sand.shadow }]
            }
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

## ArgsTable

<ArgsTable story="EmailPassword" />
